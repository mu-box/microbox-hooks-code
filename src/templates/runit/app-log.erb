#!/bin/sh -e

# create the log directory if it doesn't exist
[ -d /var/log/gomicro/<%= key %> ] || mkdir -p /var/log/gomicro/<%= key %>

# tell svlogd to drop the stdout into our custom location
cd /var/log/gomicro/<%= key %>
exec svlogd -v -ttt /var/log/gomicro/<%= key %>
